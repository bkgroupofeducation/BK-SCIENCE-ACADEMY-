import * as Lucide from 'lucide-react';
console.log(Object.keys(Lucide).filter(k => k.toLowerCase().includes('facebook') || k.toLowerCase().includes('twitter') || k.toLowerCase().includes('instagram') || k.toLowerCase().includes('linkedin') || k.toLowerCase().includes('youtube')));
